export default function ({ app, store, redirect }) {
  // const cookie = app.$cookies.get('PHPSESSID');
  //
  // // if the user try to access to the login page
  // if (app.store.$router.history.current.path === '/') {
  //   if (cookie) {
  //     // if the user is already logged, we redirect him to the dashboard
  //     return redirect('/tableau-de-bord');
  //   }
  // } else if (app.store.$router.history.current.path !== '/') {
  //   if (!cookie) {
  //     // if the user isn't already logged, we redirect him to the home page
  //     return redirect('/');
  //   } else {
  //     return redirect(app.store.$router.history.current.path);
  //   }
  // }
}
